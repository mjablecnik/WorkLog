<!--
  Placeholder root page so `svelte-kit sync` has at least one route to build a
  manifest from — SvelteKit refuses to sync a project with zero routes at all,
  which would otherwise block `bun run check` at every checkpoint before the API
  routes exist (task 8). This file is scaffolding, exactly like the four inputs
  task 1.1 already creates for the same reason; 002-worklog-ui replaces it.
-->
<p>Worklog</p>
